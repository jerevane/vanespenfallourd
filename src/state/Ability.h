// Generated by dia2code
#ifndef STATE__ABILITY__H
#define STATE__ABILITY__H

#include <map>
#include <string>

namespace state {
  class Element;
}

#include "AbilityTypeID.h"

namespace state {

  /// class Ability - 
  class Ability {
    // Associations
    // Attributes
  protected:
    std::map<int,bool> abilities;
    // Operations
  public:
    std::string LaunchAbility (int ability, Element* caster);
    std::map<int,bool> getAbility ();
    void setAbility (int ability);
    void unsetAbility (int ability);
    std::string Brazier (Element* caster);
    std::string Poison (Element* caster);
    std::string Flare (Element* caster);
    std::string FastHeal (Element* caster);
    std::string Resurrection (Element* caster);
    std::string Haste (Element* caster);
    std::string Defender (Element* caster);
    std::string CounterAttack (Element* caster);
    std::string BattleCries (Element* caster);
    std::string Bleeding (Element* caster);
    std::string TripleAttack (Element* caster);
    std::string DoubleUse (Element* caster);
    std::string MultipleArrow (Element* caster);
    std::string PoisonArrow (Element* caster);
    std::string PiercingArrow (Element* caster);
  };

};

#endif
